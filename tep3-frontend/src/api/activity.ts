export const trackActivity = (activity: any) => {
  console.log('Activity tracked:', activity);
};

export const logActivity = (activity: any) => {
  console.log('Activity logged:', activity);
};

export default { trackActivity, logActivity };
